import { createTheme } from "@mui/material";

export const theme = createTheme({
  colorSchemes: {
    dark: true,
  },
  typography: {
    fontFamily: '"Source Sans 3", Arial, Helvetica, sans-serif !important',
  },
});
